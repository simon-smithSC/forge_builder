{
  "name": "@forge/schema",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node dist/generate-json-schema.js",
    "generate:json-schema": "tsc -p tsconfig.build.json && node dist/generate-json-schema.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "yjs": "^13.6.27",
    "zod": "^3.25.67",
    "zod-to-json-schema": "^3.24.6"
  }
}
